# xcapture service configuration
# Provide additional CLI flags to the daemon via XCAPTURE_OPTS.
# Example: XCAPTURE_OPTS="--freq 5 --iterations 100"
# Default CSV output directory under /var/lib/xcapture.
XCAPTURE_OPTS="--output-dir /var/lib/xcapture"
